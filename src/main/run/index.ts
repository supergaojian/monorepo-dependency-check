import fs from 'fs';
import path from 'path';
import { IPackageJson } from '../../types';
import { PackageJson_Name } from '../../constants/index';
import { getCommonInfo } from '../_common/get-common-info';

export default function run(cwd: string, command: string) {
    
}
